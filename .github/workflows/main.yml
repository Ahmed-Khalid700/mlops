name: test

on: push

jobs:
    lint:
      runs-on: ubuntu-22.04
      steps:
        - run: echo "Linting..."

    test:
      runs-on: ubuntu-24.04
      steps:
        - run: echo "Testing..."

    build:
      needs: [lint, test]
      runs-on: ubuntu-slim
      steps:
        - run: echo"Building..."
    
