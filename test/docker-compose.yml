services:

  redis:
    image: redis:latest
    
    networks:
      - counter-net
    
    ports:
      - "6379:6379"

  count-app:
    build: .

    networks:
      - counter-net

    ports:
      - "8000:8000"
    
    depends_on:
      - redis

networks:
  counter-net:

